different experiments (fingerprints differ)

No field differs, which should not be possible for fingerprints
that differ: the diff covers every field the fingerprint hashes.
Please report this, with the output of `rlctl show` for each id.
